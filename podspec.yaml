grow_version: ">=0.0.50"

localization:
  default_locale: en
  locales:
  - en
  - ja

static_dirs:
- static_dir: /source/images/
  serve_at: /static/images/
- static_dir: /source/rsc/
  serve_at: /static/rsc/
- static_dir: /dist/
  serve_at: /static/

extensions:
  jinja2:
  - extensions.wordwrap.Wordwrap
